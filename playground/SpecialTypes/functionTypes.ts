// // descriptive way of specifying a function type, complete with signatures.
// let sumOrder: (price: number, quantity: number) => number;

// // We could then assign it some where else - hovering over it will show us the detailed signature above.
// sumOrder = (x, y): number => x * y

// const sum = sumOrder(25, 2);

// console.log(`Total sum: ${sum}`);