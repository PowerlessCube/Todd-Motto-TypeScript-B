// // the question mark indicates that quantity is optional as a param.
// let sumOrder: (price: number, quantity?: number, ) => number;

// sumOrder = (x, y) => {
//   if (y) {
//     return x * y;
//   }
//   return x;
// }

// const sum = sumOrder(25);

// console.log(`Total sum: ${sum}`);